<% if current_user.already_liked?(t) %>
    <%= link_to tweet_like_path(id: t.id, tweet_id: t.id), destroy_like_path(t), method: :delete, remote: ture do %>
    <i class="fas fa-heart"></i><%= t.likes.count %>
    <% end %>
<% else %>
    <%= link_to tweet_likes_path(id: t.id, tweet_id: t.id), create_like_path(t), method: :post, remote: true do %>
    <i class="far fa-heart"></i><%= t.likes.count %>
    <% end %>
<% end %>